<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" android:required="false" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" android:required="false" />

    <uses-feature android:name="android.hardware.camera" android:required="false" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DSP"
        tools:targetApi="31">

        <activity
            android:name=".DashboardActivity"
            android:exported="true"
            android:theme="@style/Theme.DSP">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".MainActivity"
            android:exported="false"
            android:theme="@style/Theme.DSP" />

        <activity
            android:name=".ResultActivity"
            android:exported="false"
            android:theme="@style/Theme.DSP" />

        <activity
            android:name=".SoruEkleActivity"
            android:exported="false" />

        <activity
            android:name=".OylamaActivity"
            android:exported="false" />

        <activity
            android:name=".HizliAktarActivity"
            android:exported="false" />

        <activity
            android:name=".HizliMetinDuzenleActivity"
            android:exported="false"
            android:theme="@style/Theme.DSP" />

        <activity
            android:name=".SoruSecActivity"
            android:exported="false" />

        <activity
            android:name=".KameraOcrActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|uiMode" />

        <activity
            android:name=".PdfSeciciActivity"
            android:exported="false"
            android:theme="@style/Theme.DSP" />

        <activity
            android:name=".SoruKirpiciActivity"
            android:exported="false"
            android:theme="@style/Theme.DSP" />

        <receiver
            android:name=".BildirimAlici"
            android:exported="false" />

    </application>

</manifest>